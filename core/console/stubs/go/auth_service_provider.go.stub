package providers

import (
	"github.com/zatrano/framework/core"
)

// AuthServiceProvider registers authorization gates and policies.
type AuthServiceProvider struct{}

func (p *AuthServiceProvider) Register(app *core.Application) {}

func (p *AuthServiceProvider) Boot(app *core.Application) {
	// Register gates and policies here, e.g.:
	// app.Gate().Policy("user", policies.NewUserPolicy())
}
